export default function SessionExpiredModal({ open, onReconnect }: { open: boolean; onReconnect: () => void }) { if (!open) return null; return <div className="fixed inset-0 z-50 bg-black/60 flex items-center justify-center px-6"><div className="glass metallic-border rounded-3xl p-8 max-w-lg w-full text-alabaster"><p className="text-gold uppercase tracking-[0.2em] text-sm">Session</p><h2 className="text-3xl mt-2">Votre session a expiré</h2><p className="text-alabaster/70 mt-3">Reconnectez-vous pour reprendre l’accès sécurisé à votre espace DOMYLI.</p><button onClick={onReconnect} className="mt-6 rounded-2xl border border-gold/30 px-5 py-3 text-gold hover:bg-gold/10">Reconnexion</button></div></div>; }
