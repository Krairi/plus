export default function ErrorState({ title = "Une erreur est survenue", message, retry }: { title?: string; message: string; retry?: () => void }) { return <div className="glass metallic-border rounded-3xl p-8"><h2 className="text-2xl text-red-200">{title}</h2><p className="text-alabaster/75 mt-3">{message}</p>{retry ? <button onClick={retry} className="mt-5 rounded-2xl border border-gold/30 px-4 py-2 text-gold hover:bg-gold/10">Réessayer</button> : null}</div>; }
