import { useEffect, useState } from "react";
export default function OfflineBanner() { const [offline, setOffline] = useState(!navigator.onLine); useEffect(() => { const onOffline = () => setOffline(true); const onOnline = () => setOffline(false); window.addEventListener("offline", onOffline); window.addEventListener("online", onOnline); return () => { window.removeEventListener("offline", onOffline); window.removeEventListener("online", onOnline); }; }, []); if (!offline) return null; return <div className="fixed bottom-4 left-4 right-4 z-40 rounded-2xl border border-orange-300/30 bg-orange-400/10 px-4 py-3 text-orange-100">Vous êtes hors ligne. Certaines actions peuvent être temporairement indisponibles.</div>; }
