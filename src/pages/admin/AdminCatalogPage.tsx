import AdminShell from "@/src/components/admin/AdminShell";
import { useAdminCatalog } from "@/src/hooks/admin/useAdminCatalog";
export default function AdminCatalogPage() { const { data, loading, error } = useAdminCatalog(); return <AdminShell title="Catalogue" subtitle="Vision consolidée des bibliothèques recettes et tâches.">{loading ? <div>Chargement du catalogue…</div> : null}{error ? <div className="text-red-300">{error}</div> : null}{!loading && !error ? <div className="grid md:grid-cols-2 xl:grid-cols-4 gap-5"><div className="glass metallic-border rounded-3xl p-5"><p className="text-gold/70 uppercase text-sm tracking-[0.2em]">Recettes</p><p className="text-3xl mt-2">{data.recipesCount}</p></div><div className="glass metallic-border rounded-3xl p-5"><p className="text-gold/70 uppercase text-sm tracking-[0.2em]">Tâches</p><p className="text-3xl mt-2">{data.tasksCount}</p></div><div className="glass metallic-border rounded-3xl p-5"><p className="text-gold/70 uppercase text-sm tracking-[0.2em]">Brouillons recettes</p><p className="text-3xl mt-2">{data.draftRecipesCount}</p></div><div className="glass metallic-border rounded-3xl p-5"><p className="text-gold/70 uppercase text-sm tracking-[0.2em]">Brouillons tâches</p><p className="text-3xl mt-2">{data.draftTasksCount}</p></div></div> : null}</AdminShell>; }
